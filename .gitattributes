# Executed inside Linux containers: must keep LF.
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
.env.example text eol=lf
client/nginx/*.conf text eol=lf

# start.ps1 is UTF-8 with BOM; PowerShell 5.1 needs CRLF and the BOM kept.
*.ps1 text eol=crlf
