"""UCM-8/UCM-9/UCM-10 - Zone identifiers of the two hand-written profiles.

The shared inputs and core runs (catalog, rules, profiles, resolution, gating,
prioritisation) are session fixtures in `tests/conftest.py`: the audit-log tests
(UCM-11) record the same runs these tests assert on.
"""

ZONE_OT = "Z-OT-CORRIDOR"
ZONE_SIS = "Z-SIS"
ZONE_ENG = "Z-ENG-STATION"
