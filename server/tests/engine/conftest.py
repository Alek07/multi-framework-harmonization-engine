"""Zone identifiers of the two hand-written profiles.

The shared inputs and core runs (catalog, rules, profiles, resolution, gating,
prioritisation) are session fixtures in `tests/conftest.py`.
"""

ZONE_OT = "Z-OT-CORRIDOR"
ZONE_SIS = "Z-SIS"
ZONE_ENG = "Z-ENG-STATION"
